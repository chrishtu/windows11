import { Locale } from "../interfaces/locale"

const Vi: Locale = {
  window: {
    titlebar: {
      buttons: {
        minimize: 'Thu gọn',
        maximize: 'Thu nhỏ',
        maximizeDown: 'Phóng to',
        close: 'Đóng'
      }
    }
  }
}

export default Vi
