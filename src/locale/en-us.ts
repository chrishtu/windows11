import { Locale } from "../interfaces/locale"

const En: Locale = {
  window: {
    titlebar: {
      buttons: {
        minimize: 'Minimize',
        maximize: 'Restore Down',
        maximizeDown: 'Maximize',
        close: 'Close'
      }
    }
  }
}

export default En
